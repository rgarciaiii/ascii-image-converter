# ascii-image-converter
A program which gray scales an image and converts it to Ascii based off of the darkness/lightness of a given area.

Included are sample images to get acquainted with using bigger and smaller font sizes, along with various ascii image widths.  Make sure the ascii image width does not exceed the actual width of the image.
